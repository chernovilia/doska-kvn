# Дорожная карта Доска/КВН

## Где мы сейчас (сентябрь 2026)

- Фронт на Next.js 14 задеплоен на Amvera, работает по временному Amvera-URL.
- Домен `доска-квн.рф` (Punycode: `xn----7sbhf4acwc1a.xn--p1ai`) в процессе привязки.
- DNS сейчас управляется через nic.ru (услуга «Управление зоной» включена), NS-серверы переводятся на Cloudflare для CDN и защиты.
- Данные — mock-данные из `data/mock.js` и `data/profile.js`.
- Репозиторий пока публичный (для упрощения деплоя на Amvera).

---

## ⚠️ ВАЖНО: перевести репозиторий в приватный

**Когда**: перед тем, как начнём заливать реальные данные пользователей, ключи, секреты. Ориентир — начало этапа 3 (VK-авторизация и `.env` с ключами).

**Как**:
1. Убедиться, что настроен доступ Amvera к приватному репо: https://github.com/settings/installations → Amvera → Repository access → добавить `doska-kvn`.
2. Проверить, что деплой из приватного репо всё ещё работает: сделать пустой коммит, посмотреть, что Amvera подтягивает.
3. Только после этого — переключить видимость: `gh repo edit chernovilia/doska-kvn --visibility private`.
4. Все секреты (ключи VK, ЮKassa, GigaChat, коннекты к БД) хранить в **Amvera → Переменные окружения**, а не в коде.

---

## Ближайшие шаги (эта неделя)

- [ ] Дождаться, пока Cloudflare подтвердит NS-серверы и подхватит домен.
- [ ] В Amvera → Домены → добавить `xn----7sbhf4acwc1a.xn--p1ai` и `www.xn----7sbhf4acwc1a.xn--p1ai`, дождаться выпуска Let's Encrypt.
- [ ] Проверить открытие `https://доска-квн.рф` во всех браузерах и на телефоне.
- [ ] Настроить Cloudflare Analytics и правило кеша для статики Next.js (`/_next/static/*` — cache forever).

---

## Этап 1 · Подготовка кода к бэкенду

Цель: развязать фронт и моки, чтобы бэкенд подключился одной правкой.

- [ ] Создать `lib/api.js` с функциями: `getAds()`, `getAd(id)`, `getMe()`, `getChats()`, `getMessages(chatId)`, `getReviews(userId)`.
- [ ] Внутри пока — те же моки, но интерфейс уже «серверный» (возвращают Promise).
- [ ] Переписать компоненты `app/page.jsx` и `app/profile/page.jsx` на эти функции.
- [ ] Заменить строки типа `"3 дня назад"` на ISO-timestamp + helper `formatRelative()` на фронте.
- [ ] Заменить строковые ID моков на UUID (`crypto.randomUUID()`).
- [ ] Оформить единую схему данных в `lib/types.js` — эта же схема ляжет в Prisma.

**Результат**: код готов к переключению на API за один env-переменную `NEXT_PUBLIC_API_URL`.

---

## Этап 2 · Бэкенд NestJS + Postgres

Цель: реальные объявления и профили, живущие в БД.

- [ ] Отдельный репозиторий `doska-kvn-api` (тоже приватный).
- [ ] Стек: NestJS + TypeScript + Prisma + PostgreSQL.
- [ ] Схема Prisma:
  - `User`, `Shop` (для магазинов)
  - `Ad`, `AdPhoto`
  - `Chat`, `Message`
  - `Review`, `Favorite`
  - `AdView`, `AnalyticsEvent` (сразу — под будущие рекомендации)
- [ ] REST-контроллеры: `GET/POST /ads`, `GET /ads/:id`, `GET /me`, `GET /chats`.
- [ ] На Amvera подключить **Managed PostgreSQL 16** — ~500 ₽/мес.
- [ ] Задеплоить API как второе приложение на Amvera — свой поддомен, например `api.доска-квн.рф`.
- [ ] Индексы Postgres: `(city, section, status, created_at DESC)`, полнотекст `tsvector(title || description)`.

---

## Этап 3 · Авторизация ВКонтакте и медиа

Цель: живой вход и настоящие фото.

- [ ] Зарегистрировать VK-приложение на dev.vk.com, получить `app_id` и `secure_key`.
- [ ] Подключить VK ID SDK на фронте, endpoint `POST /auth/vk` на бэке.
- [ ] JWT в `httpOnly cookie`, `SameSite=Lax`, `Secure`.
- [ ] Подключить S3 (Timeweb S3 или Selectel S3) для фото — ~100 ₽/мес.
- [ ] Endpoint `POST /upload/presign` → фронт грузит фото напрямую в S3.
- [ ] На бэке через `sharp` резать на 3 размера (thumb 300px, card 800px, full 1600px).
- [ ] Перевести репозиторий в приватный (см. пункт «⚠️ ВАЖНО» выше).

---

## Этап 4 · Мессенджер и модерация

Цель: живое общение внутри платформы и защита от спама.

- [ ] Managed Redis на Amvera — ~300 ₽/мес.
- [ ] Socket.IO с Redis-adapter на API-бэке.
- [ ] События: `chat:join`, `message:send`, `message:read`, `typing`.
- [ ] Модерация через BullMQ-очередь:
  - Черный список слов + pHash фото (проверка на дубли)
  - GigaChat/YandexGPT для проверки объявления (~1 ₽/шт)
- [ ] Мини-админка `/admin` (доступ только по твоему VK id) для ручной модерации сомнительных.

---

## Этап 5 · Монетизация и рекомендации

Цель: первый доход и удержание пользователей.

- [ ] ЮKassa: интеграция + вебхуки.
- [ ] Разовые опции: «Поднять» 39 ₽, «Срочно» 79 ₽, VIP 199 ₽.
- [ ] Подписки: ТОП-Мастер 500 ₽/мес, Сквозной КВН 1200 ₽/мес.
- [ ] Content-based рекомендации: «Похожие» на карточке объявления (по `city + section + ±30% цены`).
- [ ] События в таблицу `AnalyticsEvent` (просмотр, лайк, показ телефона, сообщение) — фундамент для будущей коллаборативной фильтрации.

---

## Этап 6 · Мобильный опыт и запуск

Цель: удобство на телефоне и первые сотни пользователей.

- [ ] PWA-манифест + service worker → «Добавить на главный экран».
- [ ] VK Mini App-режим для входа из VK без формы.
- [ ] Push-уведомления (VK Bridge для VK-юзеров, web push для остальных).
- [ ] Open Graph теги — красивые превью в чатах.
- [ ] sitemap.xml, robots.txt.
- [ ] Яндекс.Метрика.
- [ ] Открыть закрытую бету на 20–30 знакомых в Выксе.
- [ ] Партнёрство с «Подслушано Кулебаки/Выкса/Навашино» — пост о запуске.
- [ ] Затравить 300–500 объявлений вручную до массового открытия.
- [ ] Договориться с 5–10 мастерами-«якорями» на бесплатный ТОП-Мастер на 3 мес.

---

## Инфраструктура (текущая и плановая)

| Компонент | Сейчас | На этапе 2 | На этапе 4 |
|---|---|---|---|
| Фронт | Amvera Node.js | Amvera Node.js | Amvera Node.js |
| API | — | Amvera Node.js | Amvera Node.js |
| БД | — | Amvera Postgres | Amvera Postgres |
| Cache/Queue | — | — | Amvera Redis |
| Медиа | Unsplash-моки | Timeweb S3 | Timeweb S3 |
| Домен | `доска-квн.рф` | `+ api.доска-квн.рф` | тот же |
| DNS/CDN | nic.ru → Cloudflare | Cloudflare | Cloudflare |
| **Бюджет** | **~0 ₽/мес** | **~1 500 ₽/мес** | **~2 500 ₽/мес** |

Всё в РФ, соответствует 152-ФЗ.

---

## Что уже готово (для контекста)

- ✅ Next.js 14 App Router + Tailwind + Framer Motion + Lucide
- ✅ Главная: логотип-с-городом-после-слэша (Доска/КВН, Доска/Кулебаки и т.д.)
- ✅ 5 разделов: Барахолка · Услуги · Недвижимость · Авто · Афиша
- ✅ Фильтры в поповере, сетка одинаковых карточек, единый стиль
- ✅ Модалка объявления с VK/TG-кнопками и «Показать телефон»
- ✅ Пошаговая форма подачи с автомодерацией и Авито-импортом
- ✅ Тарифы для бизнеса (Старт / ТОП-Мастер / Сквозной КВН)
- ✅ Профиль: тип «Пользователь / Магазин», метрики, объявления, чаты, отзывы, настройки
- ✅ Авторизация через ВКонтакте (моковая)
- ✅ Мессенджер (интерфейс на моках)
- ✅ Нижняя навигация на мобильных
- ✅ Деплой на Amvera из GitHub
