# Дорожная карта Доска/КВН

## Где мы сейчас (21 сентября 2026)

### Работающая инфраструктура ✅

| Компонент | Статус | Адрес |
|---|---|---|
| Фронт | ✅ прод | https://доска-квн.рф (`xn----7sbhf4acwc1a.xn--p1ai`) |
| API | ✅ прод | https://api.доска-квн.рф/v1 |
| БД | ✅ прод | Managed PostgreSQL 17 на Amvera CNPG |
| Домены | ✅ через Cloudflare | Бесплатный тариф CF |
| HTTPS | ✅ Let's Encrypt на Amvera | Автопродление раз в 60 дней |
| CDN + защита | ✅ Cloudflare Proxy | Кеш `/_next/static/*` на 1 год |

### Работающая функциональность ✅

- Просмотр ленты объявлений (реальные данные из БД)
- Мультирегиональность: 8 URL-точек (`/`, `/kvn`, `/kulebaki`, `/vyksa`, `/navashino`, `/murom`, `/arzamas`, `/pavlovo`, `/sarov`)
- Фильтры по разделу, чипсам, поиску
- Блок «В соседних городах» с параллельными запросами
- Мемори-кеш на клиенте (регионы 60с, объявления 15с)
- Скелетоны при загрузке — нет пустого экрана
- Слайдер главной с редактируемыми слайдами
- Профиль (моки), мессенджер (моки), уведомления (моки)
- Тестовая авторизация (localStorage)

### Тарифы и бюджет

| Позиция | Тариф | ₽/мес |
|---|---|---|
| Фронт (Amvera Node.js) | Начальный+ (0.5 CPU / 1 GB) | 490 |
| API (Amvera Node.js) | **Начальный Плюс CPU (1 CPU / 1 GB)** | 970 |
| БД (Amvera CNPG) | Начальный+ (0.5 CPU / 1 GB) | 490 |
| Диск БД (7 GB) | | 180 |
| Домен `доска-квн.рф` | | 20 |
| **Итого** | | **~2 150 ₽/мес** |

Планируется добавить (позже):
- Managed Redis: +300 ₽
- S3 (Timeweb) для фото: +100 ₽
- GigaChat модерация: +200 ₽ (по потреблению)
- Итого через 2-3 мес: ~2 800 ₽/мес

---

## ⚠️ ВАЖНО: перевести репозитории в приватные

**Когда**: перед этапом 3 (SMS-авторизация + `.env` с секретами SMSC, VK, Yandex).

**Как**:
1. GitHub → chernovilia/doska-kvn → Settings → Visibility → Private
2. GitHub → chernovilia/doska-kvn-api → Settings → Visibility → Private
3. Убедиться, что доступ Amvera сохранён (через GitHub App)

---

## ⚠️ Безопасность и обновления

- [ ] **Апгрейд Next.js**: `14.2.5` → `14.2.30` (safe-версия) — до этапа 3.
- [ ] **npm audit fix** для транзитивных high/critical (5 vulnerabilities на фронте, 25 на бэке).
- [ ] Настроить **Dependabot** — авто-PR на security.
- [ ] После бэка: **rate limiting** через Redis на `POST /ads`, `POST /messages`, `POST /auth/*`.
- [ ] После бэка: **CORS whitelist** уже ок — оставить только основные домены.
- [ ] **CSP-заголовки** через `next.config.js` — защита от XSS.
- [ ] После этапа 3 — **Sentry** на фронт и бэк для отслеживания ошибок в проде.

---

## 🎯 Целевая аудитория и запуск

- **Первый канал**: паблик «Подслушано Кулебаки» (32 000 подписчиков, владелец — Илья). Посев 0 ₽.
- **Юридическая база**: самозанятость Ильи и его девушки — площадка как информационный посредник.
- **Целевой охват MVP**: 500–1000 активных пользователей за первые 3 месяца.
- **Расширение**: соседние города через маркетинговые домены (`доска-муром.рф`, `доска-арзамас.рф` → 301 на основной).

---

## 🔐 Авторизация — финализировано

Основной канал — **телефон + SMS через SMSC.ru**. Опция — **e-mail magic-link через Yandex SMTP**. Позже — **Яндекс ID**. VK ID НЕ используем (жёсткая модерация).

- Access token: JWT, 15 мин, `httpOnly` cookie
- Refresh token: 90 дней, `httpOnly` cookie, ротируется при каждом refresh
- **Trusted Device**: `device_id` в вечной куке — после 90 дней входим без SMS
- Экономика SMS: ~12 ₽/юзер/год со стандартной схемой, ~3 ₽/юзер с Trusted Device

---

## 🛡 Модерация — многослойная

1. **Rate limit**: 5 объявлений/день, 1 в час
2. **Blacklist слов**: ~200 стоп-слов
3. **pHash дубли фото**
4. **Levenshtein по заголовку** — >85% совпадение
5. **Ручная очередь** в админке
6. **LLM (позже, feature flag)** — GigaChat, не DeepSeek

---

## 📱 PWA-стратегия

- `manifest.json` + минимальный SW
- Иконки, iOS meta-теги
- `<InstallPWABanner>` — определяет платформу
- `<OpenInBrowserModal>` — обход VK-браузера (intent://, QR, iOS-инструкция)
- Аналитика установок: `pwa_prompted`, `pwa_installed`, `pwa_dismissed`, `pwa_open`

---

## Что уже сделано ✅

### Инфраструктура
- ✅ Домен `доска-квн.рф` + `api.доска-квн.рф` + HTTPS
- ✅ NS на Cloudflare (CDN, DDoS-защита, кеш статики Next.js на 1 год)
- ✅ Деплой фронта из GitHub → Amvera по webhook
- ✅ Деплой API из GitHub → Amvera по webhook
- ✅ Managed PostgreSQL 17 с автобэкапами Amvera
- ✅ Автоматический выпуск и продление Let's Encrypt

### Архитектура фронта
- ✅ Слой `lib/api.js` — async-функции с реальным HTTP + memory-кеш
- ✅ ISO-8601 даты, форматирование `formatRelative`, `formatEventDate`
- ✅ Типизация в `lib/types.js`
- ✅ Мульти-регион: `data/regions.js`, `data/categories.js`, `data/slides.js`
- ✅ URL-роутинг `/[place]` со статической генерацией
- ✅ Memory-кеш с TTL и параллелизация запросов

### Бэкенд
- ✅ NestJS 10 + TypeScript + Prisma 5.19
- ✅ Prisma-схема с 7 таблицами (Region, City, User, Ad, AdPhoto, Setting)
- ✅ 9 endpoints (`GET /regions`, `/cities`, `/ads`, `/ads/:id`, `/ads/counts`, `POST /ads`, `GET /me`, `/health`, root)
- ✅ CORS whitelist, helmet, ValidationPipe, глобальный prefix `/v1`
- ✅ Тестовая auth через `X-Debug-User-Id` header (заглушка перед JWT)
- ✅ Автостарт `SeedService` через `SEED_ON_STARTUP=true`
- ✅ 5 регионов + 7 городов + 5 объявлений + 11 feature flags в БД

### UI/UX
- ✅ Логотип с дропдауном «Ваш регион / Другие — скоро»
- ✅ Двухуровневые категории (30-50 подкатегорий) в фильтре и в форме подачи
- ✅ Слайдер главной (4 слайда, автопрокрутка, редактируемые через `data/slides.js`)
- ✅ Скелетоны при загрузке (никакого пустого экрана)
- ✅ Блок «В соседних городах» с градиентом и «Показать все»
- ✅ AdModal: только «Написать» + «Показать телефон», gated на auth
- ✅ `/login` — вход через SMS/email (пока моки)
- ✅ `/messages` — полноэкранный мессенджер с шапкой объявления (моки)
- ✅ Профиль: пользователь/магазин, метрики, отзывы, настройки (моки)
- ✅ Иконка сообщений в шапке на десктопе

---

## Ближайшие шаги (следующая сессия)

### Приоритет 1 — заложить бизнес-структуру

Полностью описано в [BUSINESS-MODEL.md](./BUSINESS-MODEL.md).

- [ ] **Расширить Prisma-схему** — миграция с новыми таблицами:
  - `UserType`: `personal | master | shop` (было `personal | shop`)
  - `BusinessProfile` (переименование из `Shop`) + поле `kind`
  - `Ad`: `authorType` (снапшот), `promoLevel`, `boostedAt`, счётчики
  - `Tier`, `Subscription`, `AdPromo`
  - `Banner`, `Payment`, `Wallet`, `WalletTransaction`, `Promocode`
  - `Favorite`, `AdView`, `Report`, `AnalyticsEvent`
- [ ] Обновить `lib/types.js` и `data/profile.js` (моки под новую структуру)
- [ ] Обновить UI: бейдж «Мастер/Магазин» на карточке, тумблер типа в профиле
- [ ] Реализовать формулу ранкинга в `AdsService.list()`
- [ ] Feature flags в БД: `payments.enabled = false`, все веса ранкинга

### Приоритет 2 — публичные страницы (SEO + шеринг)

- [ ] **`/ad/[id]`** — публичная страница объявления
- [ ] **`/u/[slug]`** — публичный профиль бизнес-аккаунта
- [ ] Open Graph теги для красивого превью в VK/TG

### Приоритет 3 — подготовка к запуску

- [ ] **Домены соседних регионов** — купить впрок (`доска-муром.рф`, `доска-арзамас.рф`, `доска-павлово.рф`, `доска-нн.рф` — ~200 ₽/год)
- [ ] **Форма подачи** реально сохраняет через `POST /v1/ads`
- [ ] **Обновить репозитории в приватные** — перед этапом 3
- [ ] **Страницы `/terms` и `/privacy`** — юридический минимум
- [ ] **Sentry** — отслеживание ошибок в проде

---

## Этап 1 · Подготовка кода к бэкенду ✅ (сделано)

## Этап 2 · Бэкенд NestJS + Postgres ✅ (сделано)

## Этап 3 · Авторизация (SMS + email) и медиа

- [ ] SMSC.ru: регистрация, интеграция, отправка 4-значного кода
- [ ] Redis: хранение кода (TTL 5 мин), rate limit (1 SMS/минуту на телефон)
- [ ] `POST /v1/auth/phone/request`, `POST /v1/auth/phone/verify`, `POST /v1/auth/refresh`, `POST /v1/auth/logout`
- [ ] Двухтокенная схема: access 15 мин + refresh 90 дней
- [ ] Trusted Device: вечная кука `device_id` + таблица `TrustedDevice`
- [ ] E-mail magic-link через Yandex SMTP
- [ ] S3 (Timeweb, ~100 ₽/мес) + pre-signed upload
- [ ] `sharp` — 3 размера (300 / 800 / 1600)
- [ ] Форма подачи реально сохраняет — `POST /ads` → статус `pending`
- [ ] Yandex ID подключить как второй OAuth-путь после одобрения модерации
- [ ] Страницы `/terms` и `/privacy` — под 152-ФЗ
- [ ] Перевести репо в приватные

## Этап 3.5 · Админка (MVP-версия)

Подробности в `ADMIN.md`.

- [ ] Middleware для `/admin/**` — читает `AdminUser.role` из БД
- [ ] Дашборд с базовыми метриками
- [ ] Модерация — очередь `pending`, действия «Одобрить / Отклонить / Заблокировать»
- [ ] Жалобы — таблица `Report`, обработка
- [ ] Пользователи — поиск + блок / разблок
- [ ] Объявления — все, поиск, удаление
- [ ] Слайды главной — редактор `Slide` из БД (заменяет `data/slides.js`)
- [ ] Настройки — визуальный редактор feature flags
- [ ] `AdminAction` — audit log каждого действия

## Этап 4 · Мессенджер и LLM-модерация

- [ ] Managed Redis на Amvera (~300 ₽/мес)
- [ ] Мессенджер: **polling каждые 10 сек** — без Socket.IO на MVP
- [ ] Модерация через BullMQ-очередь:
  - Slot 1-4: rate limit + blacklist + pHash + Levenshtein
  - Slot 5: **GigaChat** через feature flag
- [ ] Кнопка «Пожаловаться» → `POST /reports`

## Этап 5 · Монетизация

Полный план — [BUSINESS-MODEL.md](./BUSINESS-MODEL.md).

- [ ] ЮKassa: интеграция + вебхуки
- [ ] **Разовые опции**: Поднять 39 ₽ / Срочно 79 ₽ / VIP 199 ₽
- [ ] **Подписки**: Старт 0 ₽ / ТОП 500 ₽/мес / Премиум 1 200 ₽/мес
- [ ] **Верификация**: юр. проверка ИП/ООО 300 ₽ разово
- [ ] **Кошелёк «звёзды»** — награды за активность, эквивалент разовых опций
- [ ] **Промо-коды** — таблица `Promocode`, реферальная программа
- [ ] **Баннеры**: `Banner` со статистикой, main slide / feed inline / sidebar
- [ ] Content-based рекомендации (уже подготовлено `getSimilarAds`)
- [ ] События в `AnalyticsEvent` — фундамент для коллаборативной фильтрации
- [ ] Grace period 3 дня после истечения подписки, потом флаг `subscriptionExpired`
- [ ] Активация через feature flag `payments.enabled = true` в админке — без деплоя

## Этап 6 · Мобильный опыт и запуск

- [ ] PWA-манифест + минимальный Service Worker
- [ ] Иконки 192×192, 512×512 maskable
- [ ] iOS meta-теги
- [ ] `<InstallPWABanner>` — умный
- [ ] `<OpenInBrowserModal>` — для VK-браузера
- [ ] Аналитика PWA
- [ ] Open Graph теги для страниц объявлений
- [ ] sitemap.xml + robots.txt
- [ ] Яндекс.Метрика + цели
- [ ] Sentry + UptimeRobot
- [ ] **Наполнение до запуска**: 300+ реальных объявлений (парсер VK-групп + ручное)
- [ ] Закрытая бета: 20-30 знакомых в Выксе
- [ ] Пост в «Подслушано Кулебаки» (32k подписчиков)
